Run: 
```
npx -y gxe@latest AnEntrypoint/shelly cli serve --seed <your seed>
npx -y gxe@latest AnEntrypoint/shelly cli connect --seed <same seed>
then
npx -y gxe@latest AnEntrypoint/shelly cli send <to stdio>

```
