[phases.setup]
aptPkgs = ["build-essential", "python3", "python3-dev", "pkg-config"]

[phases.install]
dependsOn = ["setup"]
cmds = ["npm install"]

[start]
cmd = "node src/server/index.js"
port = 3000
